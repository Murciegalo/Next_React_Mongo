import dbConnect from '../../../utils';
import Product from '../../../model/Product';

export default function handler(req, res) {
  const { method } = req;

  if (method === 'GET') {
  }
  if (method === 'POST') {
  }
}
